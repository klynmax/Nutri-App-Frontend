import { Switch } from "@material-ui/core";
import React, { Fragment } from "react";

function Main(props){

    let routes = (
        <Switch>
            <Route path="/" component={} />
        </Switch>
    )

    routes = (
        <Switch>
            <Route path="/app/teste" component={Teste} />
        </Switch>
    )
}